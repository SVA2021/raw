import { HeroTitle } from "./common/Typography";

const Logo = () => {
	return (
		// <HeroTitle>
		<div className="logo">
			REST API WIDGETS (Logo)
		</div>

		// </HeroTitle>
	)
}

export default Logo;